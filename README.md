# Hello, I'm Seth.

This repository is my personal website, a TypeScript React single-page app built with Vite.

## Prerequisites

- Node.js and npm

## Setup

- `npm install`
- `npm run dev`

## Quality checks

- `npm run typecheck`
- `npm run check`
- `npm test`
- `npm run build`
